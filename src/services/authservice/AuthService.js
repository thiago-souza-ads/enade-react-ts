import axios from 'axios';

class AuthService {
    // login(login, password) {
    //     return axios.post('http://localhost:8080/login', { login, password });
    // }
}

export default AuthService;